files_to_copy:
  - .bash_profile

bash_includes:
  - .bash_includes/aliases
  - .bash_includes/helpers

docker:
  bash_completion:
    url: https://raw.githubusercontent.com/docker/cli/master/contrib/completion/bash/docker
    dest: /etc/bash_completion.d/docker

powerline:
  config:
    path: .config/powerline/config.json
